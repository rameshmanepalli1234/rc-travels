import ProfileModal from "./ProfileModal";

/** Renders customer profile modal (login is Google-only via navbar popover) */
const CustomerAuthModals = () => <ProfileModal />;

export default CustomerAuthModals;
